<Window x:Class="手术机器人上位机程序.PageTest"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:手术机器人上位机程序"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="PageTest" Height="1055" Width="1800">
    <Grid  x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" x:Name="PictureRow"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Frame x:Name="FirstPage" NavigationUIVisibility="Hidden"  Source="Page1.xaml"  Grid.Row="1" Initialized="FirstPage_Initialized">
        </Frame>
        <materialDesign:Card Grid.Row="0">
            <Button Content="隐藏向导" FontWeight="Bold" Width="100" VerticalAlignment="Center" Click="Button_Click" Cursor="Hand"/>
        </materialDesign:Card>

    </Grid>
</Window>
